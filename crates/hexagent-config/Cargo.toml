[package]
name = "hexagent-config"
version = "0.1.0"
edition = "2021"
description = "hexagent-sdk: layered TOML configuration + secrets loading."

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
anyhow = "1"
log = "0.4"
